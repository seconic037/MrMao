# Handoff: 知识库编辑器 v2（功能调整）

> 从 MrMao 项目分叉 | 2026-08-01
>
> **状态：✅ 已完结（2026-08-01）**——本交接记录的 v2 需求最终扩展为「四步工作流编辑器」：合入 master（提交 `7006087`，`tools/kb_editor.py` 930 行 + `tests/test_kb_ops.py` 24 用例 + `tests/smoke_gui.py`）。权威规格见 `docs/superpowers/specs/2026-08-01-kb-editor-v2-design.md`，实现计划见 `docs/superpowers/plans/2026-08-01-kb-editor-v2.md`。本文档保留为需求收集阶段的原始记录——下文「v1 414 行基线」与「新增只读文本查看」已过时：只读查看以「清单双击只读预览 + 知识库页只读预览区」形式吸收进 v2 工作流。

## 任务目标

在**保留 Tkinter 架构**的前提下调整 `tools/kb_editor.py`（现有 414 行，v1 已合入 master）。**已确定调整项：新增"文本查看功能"（只读，不做编辑）**——点选文件后在编辑器内展示全文内容。其余调整要求由用户另行书面提供（本会话未收到，属"待补充要求"）。

## 项目环境

- 项目路径: `C:\Users\68090\Desktop\ChairManMao`
- Python 3.11 标准库（tkinter 8.6 / ttk / threading / subprocess），零第三方依赖
- 向量库: `data/chroma_v3`（collection `maozedong-works`，6339 块），嵌入 `BAAI/bge-small-zh-v1.5`
- 启动服务: `python run_server.py` → `http://localhost:8000`
- 启动编辑器: `python tools/kb_editor.py` 或双击 `知识库编辑器.bat`

## 涉及文件

| 文件 | 类型 | 说明 |
|------|------|------|
| `tools/kb_editor.py` | 修改 | 编辑器主体（v1: 414 行）——本次调整的主要文件 |
| `docs/handoffs/2026-08-01-kb-editor-v2.md` | 新增 | 本文档 |
| `docs/superpowers/specs/2026-08-01-kb-editor-design.md` | 参考 | v1 设计规格（范围/约束基线） |
| `docs/superpowers/plans/2026-08-01-kb-editor.md` | 参考 | v1 实现计划（5 任务结构参考） |
| `README.md` | 可能修改 | 若功能/接口变化，同步更新"知识库编辑器"一节 |

## v1 现有功能（调整基线）

| 功能 | 实现位置 | 说明 |
|------|---------|------|
| 🔍 检索命中文件 | `search_files()` + `App.do_search()` | 关键词 grep，返回 文件名(N处)，双击进状态栏提示 |
| ➕ 新建文件 | `create_file()` + `App.do_create()` | 类型+文件名+内容粘贴，一次性创建 |
| 🗑 软删除 | `trash_file()` + `App.do_delete()` | 移动到 `新知识放这里/_已删除/`，同名加时间戳 |
| 🔄 重建向量库 | `App.do_rebuild()` + `_rebuild_worker()` | subprocess 跑 `run_pipeline.py`，后台线程 + `after` 回灌进度 |
| 🔁 重启服务 | `detect_service()` + `App.do_restart_service()` | `sc query` 探测候选名，nssm restart；无服务灰置 |
| 只读保护 | `is_readonly()` | `_全文.txt` 结尾 或 >1MB → 🔒 禁删 |
| 关窗保护 | `App._on_close()` | 重建中 terminate 子进程防孤儿 |

**v1 明确砍掉的能力**（v2 可能部分回归）：全文查看、全文编辑、重命名。

## 当前变更（git status 摘要，62 项未提交）

master 上有上一轮 stash 恢复的未提交改动（**与编辑器无关**，勿动）：
- `web/app.py` `web/static/app.js` `web/static/index.html` `web/static/style.css` — 场景系统 7 项改造 + 实测修复（上一轮成果，未 commit）
- `reasoning/framework.py` `reasoning/prompts/*.jinja2` `README.md` `.gitignore` — 同上
- 大量 untracked：`knowledge/framework/` `rag/knowledge_usage.py` `pipeline/scenes.py` 等知识库相关文件

**注意**：`tools/kb_editor.py` 与 `知识库编辑器.bat` 已跟踪（v1 合入），改动直接 `git add` 即可；若在 `tools/` 下**新增**文件，`.gitignore` 第 18 行忽略整个 `tools/`，需 `git add -f`。

## 约束

- 不推 GitHub（除非用户明确说）；本地 commit 可选
- 保留 Tkinter 架构，零第三方依赖（Python 3.11 标准库）
- 不改变 RAG/think 核心、日志系统、考考你题库、web/ 前端
- 不扫 `data/chroma_*` `data/extracted` `聊天记录/` `.env` `tools/nssm`
- 所有文件 UTF-8；删除仍走软删除；`_全文.txt`/大文件只读规则保留
- 两套生效机制不变：框架层变更→重启服务；语料库变更→重建向量库
- 只读查看功能：展示文件全文，**不允许编辑保存**（用户明确：不需要编辑）

## 建议加载的 Skill/文档

- Skill: `brainstorming`（先讨论 v2 功能清单再动手，尤其等用户书面要求）· `handoff-seconic`（本文档生成器）
- 文档: 本目录 `2026-08-01-kb-editor.md`（v1 分叉原文档）· `docs/superpowers/specs/2026-08-01-kb-editor-design.md`（v1 规格）
- 记忆: `kb-editor-桌面编辑器已上线`（结构/坑：tools/ 被 gitignore 需 -f）

## 待办（新会话入口）

1. 等待/收集用户书面"详细要求"（用户：我会专门写要求）
2. 用 `brainstorming` 确认 v2 功能清单（确定项：只读文本查看；其余待定）
3. 写 v2 规格 → 计划 → 实现（沿用 subagent-driven-development 流程）
