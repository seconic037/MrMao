# MrMao · 主席模拟器 — 使用帮助

## 一、环境要求

- Windows 10/11 或 macOS / Linux
- Python 3.11+（安装时勾选「Add Python to PATH」）
- 能访问 DeepSeek API（需要 API key）
- 磁盘空间约 2GB（向量模型 ~400MB + 语料索引）
- 手机同 WiFi 可访问（可选）

---

## 二、首次安装

### 1. 安装 Python 依赖

在项目目录打开终端：

```bash
pip install -r requirements.txt
```

首次会下载约 1.5GB 的依赖包，请耐心等待。

### 2. 配置 API Key

复制 `.env.example` 为 `.env`，用记事本打开，修改：

```
OPENAI_API_KEY=sk-你的DeepSeek密钥
```

> DeepSeek 注册地址：https://platform.deepseek.com
> 新用户有免费额度，约 ¥1/百万 tokens，日常使用几乎不花钱。

### 3. 构建知识库索引（仅首次）

```bash
python run_pipeline.py
```

首次运行会自动下载嵌入模型 `BAAI/bge-small-zh-v1.5`（~400MB），然后处理全部 **445 篇文章**生成向量索引（2,547 个文本块）。之后即可启动。

> 后续增加新文章只需重新运行此命令即可增量更新。

---

## 三、日常使用

### 启动服务

双击 `主席你好.bat`，或在项目目录运行：

```bash
python run_server.py
```

浏览器自动打开 `http://localhost:8000`。

### 手机访问

电脑和手机连同一个 WiFi：

- 查看电脑 IP：终端运行 `ipconfig`（Windows）或 `ifconfig`（Mac），找到 `IPv4 地址`
- 手机浏览器打开 `http://你的IP:8000`

> 如果手机无法访问，检查 Windows 防火墙是否拦截了 8000 端口。

### 聊天

点击底部 **「💬 聊天」** 标签，或首页 **「和老人家聊聊」** 按钮。

输入问题后发送，主席会用两阶段方式回应：
1. **内部思考**：矛盾分析、立场判断、主客条件分析
2. **口语表达**：用毛泽东特有的比喻和句式，自然交流

### 阅读著作

点击底部 **「📚 阅读」** 标签，按分类展开：
```
毛泽东选集（4卷133篇）→ 毛泽东文集（186篇）
→ 建国以来文稿（13篇）→ 毛泽东诗词（74首）
→ 39个知识扩展
```
点击篇名即可阅读全文。

### 查看热搜

首页「🔥 今日热议」展示百度热搜，点击任意热点可查看 **200-300 字详细概述**，看完可直接找主席聊聊看法。

### 查看聊天日志

首页或聊天页点击 **「📋 聊天日志」**，可查看当前/历史对话：
- 当前会话：浏览完整对话记录
- 历史记录：查看以往所有会话
- 支持一键 AI 总结、编辑标题、删除记录

### 疲劳度管理

聊天 21 轮后主席显示「🟡累了」，35 轮后「🔴该休息了」。点击「🍵续茶」「🚬递烟」按钮压缩对话，恢复精神继续聊。

---

## 四、增加新知识

将新的 TXT 文件放入 `data/txt/知识扩展/` 目录，重新运行：

```bash
python run_pipeline.py
```

索引会自动重建。支持任何 UTF-8 编码的纯文本文件。

---

## 五、更换 API Key

编辑 `.env` 文件，修改：

```
OPENAI_API_KEY=你的新key
```

然后重启服务即可。

---

## 六、自定义主席风格

编辑 `reasoning/prompts/speak.jinja2` 可调整：
- 说话长短（默认每次 3-5 句话）
- 口吻语气（自信从容、偶尔幽默）
- 动作描写（中括号动作的频率和类型）

编辑 `reasoning/prompts/think.jinja2` 可调整：
- 内部思考框架（主要矛盾、立场等分析维度）
- 引用知识的风格和深度

---

## 七、常见问题

**Q: 启动时报错「ModuleNotFoundError」**
A: 依赖未安装完整，运行 `pip install -r requirements.txt`

**Q: 首页热点/话题显示「加载中...」**
A: 正常现象，首次加载需等待 1-2 秒从后端获取数据。

**Q: 手机无法访问**
A: 检查电脑防火墙是否拦截了 8000 端口，或确认手机和电脑在同一 WiFi。

**Q: 对话质量不理想**
A: 可在 `reasoning/prompts/speak.jinja2` 中调整主席的说话风格，或在 `reasoning/prompts/think.jinja2` 中调整思考框架。

**Q: 向量库重建失败**
A: 删除 `data/chroma_v3/` 目录后重新运行 `python run_pipeline.py`。

---

## 八、技术架构

```
用户输入 → 向量检索(ChromaDB) + BM25 → RRF融合排序
    → 阶段1: 内部思考(think.jinja2) → 矛盾分析
    → 阶段2: 口语表达(speak.jinja2) → 毛式回答
    → 输出到前端(逐字打字效果)
```

- 后端：Python 3.11 / FastAPI / Uvicorn
- 向量库：ChromaDB (cosine 相似度)
- 嵌入模型：BAAI/bge-small-zh-v1.5
- 大模型：DeepSeek API (deepseek-chat)
- 前端：原生 HTML/CSS/JS，移动端响应式
