# 主席模拟器 · 使用帮助

## 一、环境要求

- Windows 10/11
- Python 3.11+（安装时勾选「Add Python to PATH」）
- 能访问 DeepSeek API（需要 API key）
- 磁盘空间约 2GB（向量模型 ~400MB + 语料索引）

## 二、首次安装

### 1. 安装 Python 依赖

双击运行或在项目目录打开终端：
```bash
pip install -r requirements.txt
```
首次会下载约 1.5GB 的依赖包，请耐心等待。

### 2. 配置 API Key

复制 `.env.example` 为 `.env`，用记事本打开，修改：
```
OPENAI_API_KEY=sk-你的DeepSeek密钥
```
> DeepSeek 注册地址：https://platform.deepseek.com
> 新用户有免费额度，约 ¥1/百万 tokens，日常使用几乎不花钱。

### 3. 构建索引（仅首次）

```bash
python run_pipeline.py
```
首次运行会下载嵌入模型（~400MB），然后处理全部 406 篇文章生成向量索引。之后即可启动。

## 三、日常使用

### 启动

双击 `启动主席.bat`，或在项目目录运行：
```bash
python run_server.py
```
浏览器自动打开 `http://localhost:8001`。

### 手机访问

电脑和手机连同一个 WiFi：
- 先看电脑 IP：终端运行 `ipconfig`，找到 `IPv4 地址`
- 手机浏览器打开 `http://你的IP:8001`

### 聊天

点击底部「💬 聊天」标签，或首页「和老人家聊聊」按钮。
输入问题后发送，主席会用毛选原文思考、用自己的语言回答。

### 阅读著作

点击底部「📚 阅读」标签，展开分类 → 卷 → 篇名 → 阅读全文。

### 查看日志

首页或聊天页点击「📋 聊天日志」，可查看当前/历史对话，支持一键 AI 总结和删除。

## 四、增加新文章

把新的 TXT 文件放入 `data/txt/` 对应目录，重新运行：
```bash
python run_pipeline.py
```
索引会自动重建。

## 五、更换 API Key

编辑 `.env` 文件，修改 `OPENAI_API_KEY=你的新key`，重启服务。

## 六、常见问题

**Q: 启动时报错「ModuleNotFoundError」**
A: 依赖未安装完整，运行 `pip install -r requirements.txt`

**Q: 首页热点/话题显示「加载中...」**
A: 正常现象，首次加载需等待 1-2 秒从后端获取数据。

**Q: 手机无法访问**
A: 检查电脑防火墙是否拦截了 8001 端口，或确认手机和电脑在同一 WiFi。

**Q: 对话质量不理想**
A: 可在 `reasoning/prompts/speak.jinja2` 中调整主席的说话风格。
