# 📚 MrMao 知识库编辑器介绍

> 工作流式的知识库维护桌面工具 —— 把「丢新知识 → 分类 → 确认 → 同步生效」做成四步向导，告别命令行。

## 这是什么

知识库编辑器是一个 **Tkinter 桌面应用**（零第三方依赖，双击即用），用来维护 MrMao 主席模拟器的知识库：

- **框架层**（`knowledge/framework/*.md`）：世界观、方法论等结构化知识，改动后**重启服务**即生效
- **语料库**（`data/txt/知识扩展/*.txt`）：原始语料，改动后需要**重建向量库**（3-10 分钟）才能被检索

它把原来散装的命令行操作（丢文件 → 跑 ingest 脚本 → 重建向量库 → 重启服务）整合成一个**四步工作流**，并全程遵循「**落盘前磁盘零改动**」：先看清单、调目标，确认后才真正写盘，写盘失败自动回滚。

## 快速开始

双击根目录 **`知识库编辑器.bat`**（等价于 `python tools/kb_editor.py`），主窗口打开后包含两个标签页：

```
📚 MrMao 知识库编辑器                [🟢 服务器运行中] [🔁 重启服务器]
┌──────────┐ ┌──────────┐
│ 🔧 工作流 │ │ 📚 知识库 │
│ [①新知识]→[②待上传清单]→[③本次更改]→[④同步与生效]
└──────────────────────────────────────┘
  （状态栏）
```

## 四步工作流

### ① 新知识 —— 把新文件丢进「新知识放这里」后点「🔄 刷新资料库」

自动完成三件事，逐文件列出：

| 检测项 | 说明 |
|--------|------|
| **分类** | `.md → 🧠 框架`、`.txt → 📖 语料库` |
| **去重** | 比对库内同名 / 同主干（如 `经济学原理_Marshall_BTS` vs `…_ST`）/ 首 500 字内容相似，命中标 `⚠️`（仅警示，不阻断） |
| **大小** | 显示 KB/MB |

- 行内操作：**移除**（本次不处理）、**双击**（只读预览全文）
- 有文件时「下一步 →」才可用

### ② 待上传清单 —— 确认每个文件的去向

- 每个 `.md` 可调整**分类目标**：下拉选择「合并到 经济学.md / 哲学.md / … / 新建独立文件」
  - 默认：按文件名关键词自动推断主题文件；目标已存在 → 合并追加，不存在 → 新建
- `.txt` 固定进语料库，不可改
- 行内操作：移除、双击预览；可「← 上一步」返回

### ③ 本次更改清单 —— 汇总新增 + 删除（纯草稿，磁盘不动）

- 上半：**➕新增**（来自②的确认清单）
- 下半：**🗑删除**（来自知识库页标记的文件）
- 顶部汇总：`本次共新增 N / 删除 M`

### ④ 同步与生效 —— 点「▶ 开始上传」才真正落盘

确认后按序执行，任一步失败自动回滚并提示：

1. `.md` → 合并进 `knowledge/framework/` 或新建独立文件
2. `.txt` → 复制进 `data/txt/知识扩展/`
3. 删除文件 → 软删除到 `新知识放这里/_已删除/`（同名加时间戳）
4. 收件箱原文件 → 归档到 `新知识放这里/_已处理/`

然后**自动判断生效方式**：

- 仅框架改动 → 重启服务器（几秒）
- 含语料改动（新增或删除语料）→ 重建向量库（3-10 分钟，日志实时回灌）→ 成功后重启服务

完成提示：**「✅ NPC 已拥有新思想」**。

## 知识库页

左侧两棵树（🧠 框架层 / 📖 语料库，标注生效方式），右侧只读预览：

- **浏览**：点选文件即预览全文（UTF-8 读取失败会提示，不会崩）
- **检索**：关键词全库搜索，自动定位并预览首个命中文件
- **🗑 标记删除**：选文件标记删除 → 进入步骤③的删除清单；再点一次取消标记（树中 `🚫` 显示）；🔒 只读文件拒绝删除
- **🔄 刷新**：重新扫描库文件

## 设计原则

| 原则 | 说明 |
|------|------|
| **草稿先行** | 上传前磁盘文件一律不动，随时可改主意 |
| **原子落盘** | 全部成功才归档；任一步失败回滚已执行操作，日志红字提示 |
| **只读保护** | `_全文.txt` 结尾或 >1MB 的文件 🔒 禁删（防误删卷本/大文件） |
| **软删除** | 删除只移到 `新知识放这里/_已删除/`，可手动找回 |
| **零依赖** | Python 3.11 标准库（tkinter/threading/subprocess），不装任何第三方包 |

## 常见问题

| 问题 | 处理 |
|------|------|
| 重建向量库失败 | 语料已落盘但库未更新：按日志提示手动运行 `python run_pipeline.py` 重建 |
| 服务器 30 秒没起来 | 日志提示手动处理：`python run_server.py` |
| 关窗后重建还在跑 | 不会——关窗会终止重建子进程，防孤儿 |
| 双击 bat 没反应/闪退 | 确认 Python 3.11 已安装并在 PATH；bat 出错时会停留显示提示 |

## 技术说明

- **入口**：`知识库编辑器.bat` → `python tools/kb_editor.py`（930 行，单文件）
- **测试**：`python -m unittest tests.test_kb_ops -v`（25 用例，含回滚回归）+ `python tests/smoke_gui.py`（GUI 冒烟）
- **文档**：设计规格 `docs/superpowers/specs/2026-08-01-kb-editor-v2-design.md` · 实现计划 `docs/superpowers/plans/2026-08-01-kb-editor-v2.md`
- 命令行工具 `tools/ingest_knowledge.py` 与 `归并新知识.bat` 保留不变，可继续使用
